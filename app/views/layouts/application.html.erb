<!DOCTYPE html>
<html>
<head>
  <title>Sitenotifier</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
  
  <% unless current_announcements.empty? %>
  <div id="announcement">
    <% for announcement in current_announcements %>
      <p><%=h announcement.message %></p>
    <% end %>
    <p><%= link_to "ignore this announcement", dismiss_path, :remote => true %></p>
  </div>
  <% end %>
  
  <%= yield %>

</body>
</html>
